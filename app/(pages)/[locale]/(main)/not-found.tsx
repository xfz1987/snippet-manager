import Link from 'next/link';

export default function NotFound() {
	return (
		<main className="flex h-full flex-col items-center justify-center gap-2 text-white">
			<h2 className="text-xl font-semibold">--- No Data ---</h2>
		</main>
	);
}
